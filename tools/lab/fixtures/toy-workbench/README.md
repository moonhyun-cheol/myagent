# Toy Workbench (lab)

Minimal sandbox for MY Agent full-surface pattern / chain backtests.

- Mutate target: append markers under `notes/`
- Market/brand: use product modes `cqr_market` / `cqr_concept` (not code edit)
- Image: mode `image_gen` (prompt only; no file force)
- Code demo: `src/counter.js` + `public/index.html`

Do not treat this folder as production product UI.
