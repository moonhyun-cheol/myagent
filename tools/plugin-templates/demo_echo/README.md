# demo_echo plugin template

Install into a CQR install:

```text
plugin_scaffold id=demo_echo
# or copy tools/plugin-templates/demo_echo → data/agent-plugins/demo_echo
# then index.json enable
```

API: `POST /agent-plugins` with body from this folder (see verify / dispatch).
