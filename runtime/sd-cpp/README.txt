# stable-diffusion.cpp runtime (optional)

Place Windows binaries here for local image generation:

- `sd.exe`
- required DLLs

Without this folder, MY Agent uses a placeholder PNG stub for image generation.
